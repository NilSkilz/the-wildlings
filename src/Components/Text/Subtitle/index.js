export default function SubTitle({ children }) {
  return (
    <div
      style={{
        color: "#efc429",
        fontFamily: "permanent marker",
        fontSize: "15px",
      }}
    >
      {children}
    </div>
  );
}
